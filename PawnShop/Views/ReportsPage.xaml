<UserControl x:Class="PawnShop.Views.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Отчёты и аналитика"
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Фильтры по дате -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <DatePicker x:Name="StartDatePicker"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                            materialDesign:HintAssist.Hint="Начальная дата"
                            Margin="0,0,10,0"/>
                <DatePicker x:Name="EndDatePicker"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                            materialDesign:HintAssist.Hint="Конечная дата"
                            Margin="0,0,10,0"/>
                <Button Content="Обновить"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="UpdateReports_Click"
                        Height="30"
                        Margin="0,0,0,0"/>
            </StackPanel>

            <!-- Общая сумма транзакций -->
            <materialDesign:Card Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Общая сумма транзакций:" FontSize="16"/>
                    <TextBlock x:Name="TotalTransactionsAmount" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Количество активных залогов -->
            <materialDesign:Card Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Активных залогов:" FontSize="16"/>
                    <TextBlock x:Name="ActivePledgesCount" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Активность сотрудников -->
            <materialDesign:Card Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Активность сотрудников:" FontSize="16"/>
                    <DataGrid x:Name="EmployeeActivityGrid" AutoGenerateColumns="False" Margin="0,5,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Количество транзакций" Binding="{Binding TransactionCount}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>