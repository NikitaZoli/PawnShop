<UserControl x:Class="PawnShop.Views.TransactionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PawnShop.Views"
             Background="#F5F5F5"
             mc:Ignorable="d">
    <UserControl.Resources>
        <!-- Стиль кнопки -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль текстового поля -->
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль радиокнопок -->
        <Style x:Key="RadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="RadioBorder" 
                                    Width="18" 
                                    Height="18" 
                                    CornerRadius="9" 
                                    BorderThickness="2" 
                                    BorderBrush="#BDC3C7" 
                                    Background="White">
                                <Ellipse x:Name="RadioMark" 
                                         Width="10" 
                                         Height="10" 
                                         Opacity="0"
                                         Fill="#3498DB"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                            Margin="8,0,0,0" 
                                            VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioMark" Property="Opacity" Value="1"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стили DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEEEEE"/>
            <Setter Property="VerticalGridLinesBrush" Value="#EEEEEE"/>
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="ColumnHeaderHeight" Value="42"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#ECF0F1"/>
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Управление транзакциями" 
                   FontSize="24" 
                   FontWeight="SemiBold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,16"/>

        <!-- Панель инструментов и поиска -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="6" 
                Padding="20" 
                Margin="0,0,0,16"
                BorderBrush="#EEEEEE"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Верхний ряд: Кнопка добавления и поле поиска -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Кнопка добавления -->
                    <Button Content="Добавить транзакцию" 
                            Click="AddTransactionButton_Click" 
                            Style="{StaticResource ButtonStyle}"
                            Height="32"
                            Grid.Column="0"
                            Margin="0,0,16,0">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="+" FontWeight="Bold" Margin="0,0,8,0"/>
                                    <TextBlock Text="Добавить транзакцию"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Поле поиска -->
                    <Border Grid.Column="1" 
                            BorderBrush="#BDC3C7" 
                            BorderThickness="1" 
                            CornerRadius="4" 
                            Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                                  Fill="#95A5A6"
                                  Width="16"
                                  Height="16"
                                  Margin="10,0,0,0"
                                  VerticalAlignment="Center"/>
                            <TextBox x:Name="SearchTextBox"
                                     Grid.Column="1"
                                     Height="32"
                                     BorderThickness="0"
                                     Padding="8,5"
                                     VerticalContentAlignment="Center"
                                     Text="Поиск..."
                                     Foreground="Gray"
                                     GotFocus="SearchTextBox_GotFocus"
                                     LostFocus="SearchTextBox_LostFocus"
                                     TextChanged="SearchTextBox_TextChanged" 
                                     Background="Transparent"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Нижний ряд: Фильтры поиска -->
                <StackPanel Grid.Row="1" 
                            Orientation="Horizontal" 
                            Margin="0,16,0,0">
                    <TextBlock Text="Искать:" 
                               Foreground="#34495E" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center" 
                               Margin="0,0,16,0"/>

                    <RadioButton Content="По ID Транзакции" 
                                 Name="SearchByTransactionIdRadio" 
                                 IsChecked="True" 
                                 Style="{StaticResource RadioButtonStyle}"
                                 Checked="SearchRadioButton_Checked"/>

                    <RadioButton Content="По ID Сотрудника" 
                                 Name="SearchByEmployeeIdRadio" 
                                 Style="{StaticResource RadioButtonStyle}"
                                 Checked="SearchRadioButton_Checked"/>

                    <RadioButton Content="По ID Залога" 
                                 Name="SearchByPledgeIdRadio"
                                 Style="{StaticResource RadioButtonStyle}"
                                 Checked="SearchRadioButton_Checked"/>

                    <RadioButton Content="По типу"
                                 Name="SearchByTypeRadio"
                                 Style="{StaticResource RadioButtonStyle}"
                                 Checked="SearchRadioButton_Checked"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid с транзакциями -->
        <DataGrid Name="TransactionsDataGrid" 
                  Grid.Row="2"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Сотрудника" 
                                  Binding="{Binding EmployeeId}" 
                                  Width="100"/>
                <DataGridTextColumn Header="ID Транзакции" 
                                  Binding="{Binding TransactionID}" 
                                  Width="100"/>
                <DataGridTextColumn Header="ID Залога" 
                                  Binding="{Binding PledgeID}" 
                                  Width="100"/>
                <DataGridTextColumn Header="Тип" 
                                  Binding="{Binding TransactionType}" 
                                  Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TransactionType}" Value="Залог">
                                    <Setter Property="Foreground" Value="#27ae60"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TransactionType}" Value="Выкуп">
                                    <Setter Property="Foreground" Value="#2980b9"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding TransactionType}" Value="Продление">
                                    <Setter Property="Foreground" Value="#f39c12"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Дата" 
                                  Binding="{Binding TransactionDate, StringFormat={}{0:dd.MM.yyyy}}" 
                                  Width="120"/>
                <DataGridTextColumn Header="Сумма" 
                                  Binding="{Binding Amount, StringFormat={}{0:N2} ₽}" 
                                  Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>